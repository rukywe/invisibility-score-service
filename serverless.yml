service: invisibility-score-service

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

functions:
  superheroHandler:
    handler: dist/handler.superheroHandler
    events:
      - http:
          path: superhero
          method: post
